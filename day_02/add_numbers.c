#include <stdio.h>

int main() {
    // Declare two numbers and a variable for the result
    int num1 = 5;
    int num2 = 3;
    int result;

    // Add the numbers
    result = num1 + num2;

    // Print the result
    printf("Result: %d\n", result);

    return 0;
}

